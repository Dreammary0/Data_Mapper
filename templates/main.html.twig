<!doctype html>
<html lang="ru">
<head>
  <meta charset="utf-8" />
  <link href="icon.png" rel="icon" type="image/x-icon">  
  <title>TABLE</title>
</head>
<body>
<table>
  <thead>
    <tr>
      <td>ID</td>
      <td>Наименование</td>
      <td>Описание</td>
      <td>Цена</td>
    </tr>
  </thead>
  
    <tbody>
    {%  for row in info %}
    <tr>
        <td>{{row.id}}</td>
        <td>{{row.name}}</td>
        <td>{{row.tale}}</td>
        <td>{{row.price}}</td>
    </tr>
    {%  endfor %}
    </tbody>
</table>
<p>
<p>
 
</body>
</html>


<style>

table {width: 100%; text-align: center; border-bottom: 2px solid #dfdfdf; border-radius: 6px; border-collapse: separate; border-spacing: 0px;}
table thead tr {color: #ffffff;  font-weight: bold; background: #c83240;}
table tr td {border-right: 1px solid #dfdfdf;padding: 6px;}
table tr td:last-child {border-right: 0px;}
table tbody tr:nth-child(1n) {background: #f6f6f6;}
table tbody tr:nth-child(2n) {background: #e6e6e6;}
table tbody tr:hover {background: #ffe8e8; transition-duration: 0.6s;}

*, *::before, *::after {
  box-sizing: content-box;
}


.log{
  padding: 6px;
  text-align: center; 
  font-family: inherit; 
  font-size: inherit; 
  line-height: inherit;
}

input[type=text]{
   width:250px;
    height: 30px;
    text-align: center;
}

.botton {border-radius: 4px; padding: 5px}
.botton:hover {
    background-color: #c83240; /* Red */
    color: white;
    
   } 


</style>